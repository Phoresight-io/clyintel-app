# Migration Log

Migrations applied to `clyintel-dev` (`mhvuqjryesjsrictesuk`) via Supabase MCP.
Newest first.

| Date | Name | Summary |
|---|---|---|
| 2026-09-13 | `alter_fee_settlements_add_charging_claim` ([sql](./alter_fee_settlements_add_charging_claim.sql)) | Applied via Supabase MCP, 2026-09-13. Monthly Settlement Sweep (Prompt 4) — single-flight claim for the charge cron. `ALTER TABLE public.fee_settlements` (1) drop+re-add `fee_settlements_status_check` to add `'charging'` to the allowed status set (now `pending, charging, invoiced, paid, failed, void`) and (2) `ADD COLUMN claimed_at timestamptz` (null until claimed). The charge drainer claims a row by compare-and-set `pending/failed → 'charging'` with `claimed_at = now()`; a stale `'charging'` (claimed_at older than the reclaim timeout, e.g. a crashed run) becomes claimable again, so two overlapping cron runs never both finalize/pay one settlement. No DB lock held across Stripe calls. Types regenerated into `clyintel/types/supabase.ts` (adds `claimed_at`). Security advisor after apply: no new findings. Additive + replay-safe (`drop constraint if exists` / `add column if not exists`); `rev_share_ledger` untouched. |
| 2026-09-13 | `create_persist_fee_settlement_rpc` ([sql](./create_persist_fee_settlement_rpc.sql)) | Applied via Supabase MCP, 2026-09-13. Monthly Settlement Sweep (Prompt 3) — ATOMIC persist. `CREATE OR REPLACE FUNCTION public.persist_fee_settlement(p_subscriber_id uuid, p_cycle_close date, p_total_fee_cents bigint, p_currency text, p_line_count int, p_stripe_idempotency_key text, p_lines jsonb) RETURNS TABLE(settlement_id uuid, created boolean)`. Inserts the `fee_settlements` row (status `'pending'`) AND its `fee_settlement_lines` in ONE transaction (a plpgsql body is one tx), honoring both unique guards with `ON CONFLICT DO NOTHING` — so a crash can't leave a settlement without its lines, and a re-run neither duplicates the settlement nor double-links a ledger row. Returns the settlement id + whether THIS call created it. **`SECURITY INVOKER`** + `SET search_path = ''` (all refs fully-qualified); `EXECUTE` revoked from public/anon/authenticated and granted only to `service_role` — so only the service role (BYPASSRLS) can actually write (an authenticated caller is still blocked by the tables' RLS). Prompt 2's `persistSettlements` now calls this RPC instead of the two-call upsert. Types regenerated into `clyintel/types/supabase.ts` (adds `Database['public']['Functions'].persist_fee_settlement`). Security advisor after apply: no new findings (function has a pinned search_path). Additive + replay-safe (`create or replace`); no table/column change, `rev_share_ledger` untouched. |
| 2026-09-13 | `create_fee_settlements` ([sql](./create_fee_settlements.sql)) | Applied via Supabase MCP, 2026-09-13. Monthly Settlement Sweep — the settlement layer, strictly downstream of `rev_share_ledger` (ledger stays append-only; NO `billed`/`settlement_id` column added to it). `CREATE TABLE public.fee_settlements` (id, subscriber_id FK → `subscribers.id`, cycle_close date, total_fee_cents bigint default 0, currency text default `'USD'`, status `CHECK in (pending,invoiced,paid,failed,void)` default `pending`, stripe_invoice_id, stripe_idempotency_key, line_count int, attempts int default 0, max_attempts int default 5, last_error, created_at/updated_at; non-negative `CHECK`s on the money/count columns). **UNIQUE `(subscriber_id, cycle_close)`** is the double-charge guard — one settlement per subscriber per monthly cycle. `CREATE TABLE public.fee_settlement_lines` (id, settlement_id FK → `fee_settlements.id` `ON DELETE CASCADE`, ledger_row_id FK → `rev_share_ledger.id`, fee_cents bigint, created_at) — the join table that keeps the ledger append-only; **UNIQUE `(ledger_row_id)`** ⇒ a ledger row links to at most one settlement, so the Prompt-2 selection predicate is `id NOT IN (select ledger_row_id from fee_settlement_lines)`. Indexes: `uq_fee_settlements_subscriber_cycle`, `idx_fee_settlements_status`, `idx_fee_settlements_cycle_close`, `uq_fee_settlement_lines_ledger_row`, `idx_fee_settlement_lines_settlement`. `updated_at` auto-touch via existing `public.update_updated_at()` trigger (`trg_fee_settlements_updated_at`; lines has no updated_at). RLS is **subscriber-owned read + service-role write** (re-postured from the first cut's g2 internal deny-all via follow-up migration `fee_settlements_subscriber_owned_read_rls`, 2026-09-13): both tables RLS-enabled and `FORCE`d per `rls_hardening_g3_force_rls_subscriber_owned`, each with a single **SELECT-only** `subscriber_isolation_select` policy for `authenticated` — `fee_settlements` on `subscriber_id = auth.uid()` (house mapping, same as `invoices`/`rev_share_ledger`), `fee_settlement_lines` scoped through its parent via the `EXISTS (… fee_settlements s WHERE s.id = settlement_id AND s.subscriber_id = auth.uid())` idiom (mirrors `invoice_cadence_progress`). NO insert/update/delete policy ⇒ writes closed to anon/authenticated; the sweep writes via `service_role` (BYPASSRLS). A subscriber can read their own fee billing history; nobody else can. Types regenerated into `clyintel/types/supabase.ts`. Security advisor after apply: no new findings. Additive + replay-safe (`if not exists` / `drop trigger if exists`); rev_share_ledger untouched. Schema only — Stripe charge logic, cron, and the selection query land in Prompts 2–4. |
| 2026-08-05 | `create_ledger_sync` ([sql](./create_ledger_sync.sql)) | Applied via Supabase MCP, 2026-08-05. `CREATE TABLE public.ledger_sync` — DB-side idempotency substrate for D2 Phase 1 payment writeback (reflect). Columns: id, ledger_row_id (FK → `rev_share_ledger.id`), source_payment_id, provider, external_payment_id (QBO Payment Id on success), status `CHECK in (pending,done,failed,dead)` default `pending`, attempts int default 0, max_attempts int default 5, last_error, created_at/updated_at. **Composite UNIQUE `(source_payment_id, provider)`** is the dedupe grain (mirrors `uq_rev_share_ledger_source_payment` on `(source, source_payment_id)`; `provider` is the discriminator). Indexes on `(status)` and `(ledger_row_id)`. RLS enabled with **no policies** (deny-all non-service, mirrors `webhook_events`). Types regenerated into `clyintel/types/supabase.ts`. Security advisor: only expected INFO `rls_enabled_no_policy` (intended deny-all posture). Additive + replay-safe (`if not exists`). |
| 2026-07-03 | `fix_sync_unique_indexes_non_partial` ([sql](./fix_sync_unique_indexes_non_partial.sql)) | Applied via Supabase MCP, 2026-07-03. Recreates the `(subscriber_id, source, external_id)` unique indexes on `clients` and `invoices` WITHOUT the partial `WHERE external_id IS NOT NULL` predicate added by `add_clients_source_and_sync_unique_indexes`. A column-only `ON CONFLICT (subscriber_id, source, external_id)` — all that supabase-js `.upsert({ onConflict })` can emit — cannot use a partial index as its conflict arbiter (Postgres `42P10`), which blocked the QBO sync upsert (`/api/qbo/sync`). The predicate was unnecessary anyway: a standard unique index already treats NULLs as distinct, so manual rows with a null `external_id` still never collide. Pure index swap — no column, constraint, or data change. Verified against `clients`: a real idempotent `ON CONFLICT` roundtrip collapses duplicates to one row while two null-`external_id` rows coexist. |
| 2026-07-03 | `add_clients_source_and_sync_unique_indexes` ([sql](./add_clients_source_and_sync_unique_indexes.sql)) | Applied via Supabase MCP, 2026-07-03. `ALTER TABLE public.clients ADD COLUMN source text NOT NULL DEFAULT 'manual'` (mirrors `invoices.source`; existing rows backfilled to `'manual'` by the default) + two partial unique indexes on `(subscriber_id, source, external_id) WHERE external_id IS NOT NULL` for `clients` and `invoices`. Backs the idempotent `ON CONFLICT (subscriber_id, source, external_id)` upsert the QBO sync route (`/api/qbo/sync`, Prompt 2b) needs; scoped per-subscriber because a QBO `external_id` is only unique within a realm. Purely additive — no data rewrite, no existing columns/constraints/other tables changed. `clyintel/types/supabase.ts` to be regenerated to pick up `clients.source` (unique indexes don't surface in generated types). |
| 2026-07-01 | `seed_capture_source_qbo` ([sql](./seed_capture_source_qbo.sql)) | `INSERT ... ON CONFLICT (id) DO NOTHING` registering the `qbo` capture source (`QuickBooks Online`). Idempotent — the row already existed from `capture_detection_phase1` (kind `native_adapter`), so this apply was a no-op that leaves the existing row untouched; committed to keep the repo the source of truth for the D2 webhook worker's source routing. |
| 2026-07-01 | `create_webhook_events` ([sql](./create_webhook_events.sql)) | `CREATE TABLE public.webhook_events` — generic durable webhook-event queue behind D2 capture (id, source, raw_payload jsonb, signature, status `CHECK in (pending,done,failed,dead)` default `pending`, attempts int default 0, last_error, result, received_at default now(), processed_at). Index on `(status)` for the worker claim query. RLS enabled with **no policies** — writes flow exclusively through the service role (mirrors `rev_share_ledger`'s service-role-write posture); no subscriber-facing read since it's an internal queue with no owner column. Security advisor: only expected INFO `rls_enabled_no_policy` (intended deny-all-non-service posture); no WARN/ERROR. |
| 2026-06-28 | `capture_detection_phase1` ([sql](./capture_detection_phase1.sql)) | `CREATE TABLE public.capture_sources` registry (RLS: authenticated SELECT; seeded with `qbo`) + `rev_share_ledger` attribution columns (`source` FK → capture_sources, `source_payment_id`, `source_invoice_id`) + unique idempotency index on `(source, source_payment_id)`. Types regenerated into `clyintel/types/supabase.ts`. |
| 2026-06-28 | `create_rev_share_ledger` ([sql](./create_rev_share_ledger.sql)) | `CREATE TABLE public.rev_share_ledger` — persistence layer behind the D2 accrual ledger (id, subscriber_id FK → subscribers, invoice_ref, invoice_face_value, dollars_recovered, band, rate, fee_amount, captured_at, cycle_close, engine_version, created_at; non-negative `CHECK`s on amounts, `rate` constrained to [0,1]). Indexes on `(subscriber_id, cycle_close)` and `(invoice_ref)`. RLS enabled with a **SELECT-only** `subscriber_isolation_select` policy (`subscriber_id = auth.uid()`) — intentionally NOT cmd ALL: subscribers read their own rows, writes flow only through the service role (detection job). Table ships empty; D2 BillingTab stays on mock data. Security advisor: no new findings. |
| 2026-06-19 | `add_payments_stripe_event_id_unique` ([sql](./add_payments_stripe_event_id_unique.sql)) | `ALTER TABLE public.payments ADD CONSTRAINT payments_stripe_event_id_key UNIQUE (stripe_event_id);` — DB-enforced idempotency for subscription payment capture (one payments row per Stripe event). NULLs stay distinct, column stays nullable. Table empty at apply time. Constraint was already present in clyintel-dev; the committed SQL is replay-safe (adds only if missing). No type change (UNIQUE constraints don't surface in generated types). |
| 2026-06-19 | `add_refunded_amount_cents_to_payments` | `ALTER TABLE public.payments ADD COLUMN refunded_amount_cents bigint NULL;` — cumulative refunded amount (cents); NULL = never refunded. Full-vs-partial derived as `refunded_amount_cents` vs `amount_cents` (no new enum value). Populated by the refund handler in a later prompt. Types regenerated into `clyintel/types/supabase.ts`. |
